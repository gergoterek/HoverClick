# Test Matrix

| Test | Method | Expected Result |
| --- | --- | --- |
| Release test plan is completed | Follow `docs/RELEASE_TEST.md` before tagging or publishing a source-first release candidate. | Manual release, Accessibility, focus, menu, diagnostics, Launch at Login, internal DMG, and performance sanity checks are complete or documented with clear exceptions. |
| Published v0.4.6 GitHub DMG smoke test passed | Review the recorded post-release result for the downloaded GitHub release asset `HoverClick-0.4.6.dmg`. | SHA-256 matched `4e31b9196458e326bc794dbeb33525ce4a8d2b58fe463de0e9c3c789d3a6c076`; launch worked; menu opened; version was `0.4.6` / build `32`; Accessibility status was correct; Left Click Focus worked; Right Click Focus worked when enabled; diagnostics background-focus fields worked; Finder context-menu follow-up left-click worked; Bartender/menu-bar overlay pass-through worked; Copy Summary worked; Quit/Cmd+Q worked; and the user confirmed the DMG-installed app worked normally. |
| v0.4.6 closed release scope is documented | Inspect `Info.plist`, `docs/CURRENT_STATE.md`, `docs/ARCHITECTURE.md`, and this matrix before starting later release work. | Version/build were `0.4.6` / `32`; v0.4.6 is documented as fully released, validated, and closed with the overlay/system UI skip ordering fix, preserved menu/status/Bartender overlay protection, last non-menu focus action/skip diagnostics, overlay/system UI skip reason and candidate diagnostics, last eligible hit-test candidate diagnostics, Copy Summary preservation of useful last non-menu focus results, and no runtime feature expansion beyond the already-validated fix. |
| v0.4.7 public release is documented as superseded | Inspect README and docs before starting later release work. | v0.4.7 / build 33 remains documented as the previous public release and is superseded by v0.5.0 / build 34. |
| v0.5.0 public release is documented as superseded | Inspect README and docs before starting later release work. | v0.5.0 / build 34 remains documented as a public release superseded by v0.6.0 / build 35; public DMG is `HoverClick-0.5.0.dmg`; app identity remains HoverClick / `com.gergoterek.HoverClick`; signing identity remains `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`; the event tap mask remains left mouse down + right mouse down only. |
| v0.6.0 public release is documented | Inspect README and docs before later release work. | v0.6.0 / build 35 is documented as a previous packaging and DMG presentation polish release; app identity remains HoverClick / `com.gergoterek.HoverClick`; signing identity remains `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`; the event tap mask remains left mouse down + right mouse down only. |
| v0.7.0 validated baseline is documented | Inspect `HoverClick.mm`, README, and docs before release work. | v0.7.0 / build `36` is documented as the validated baseline before v0.8.0; Right Click Focus remains default OFF, left-click behavior remains stable, diagnostics distinguish right-click observation, disabled-setting skips, ineligible-target skips, focus attempts, AX results, immediate verification, delayed verification, and last verified success; long-run diagnostics and the Window Server pointer-like pass-through fix are included; Key Focus, Caps Lock Focus, Hover Click Assist implementation, Click-Time Hover Assist, Excluded Apps, Scroll Focus, Finder selection hack, synthetic clicks, event replay, cursor movement, and expanded event handling are excluded. |
| v0.8.0 Sparkle dependency is embedded and signed | Run `scripts/build-app.sh`, then `scripts/verify-app.sh`. | The build downloads only the official pinned Sparkle `2.9.3` archive into ignored `tmp/sparkle/`, verifies SHA-256 `74a07da821f92b79310009954c0e15f350173374a3abe39095b4fc5096916be6`, embeds `Sparkle.framework` in `HoverClick.app/Contents/Frameworks/`, links the app binary with `@rpath/Sparkle.framework`, and signs Sparkle plus nested helpers with the configured Apple Development identity. No Sparkle archive, private key, appcast, release asset, or DMG is tracked or staged. |
| v0.8.0 manual update menu appears | Manual Finder UI validation -- not run automatically. Launch with `scripts/run-app.sh`, open the HoverClick status menu, and click `Updates` > `Check Now...`. | The `Check Now...` item appears under the top-level Updates section; selecting it uses Sparkle's standard user-initiated update UI against the published appcast and does not trigger an automatic update check or background install on launch. |
| v0.8.0 Sparkle configuration is manual-only | Inspect the built app's `Info.plist` with `scripts/verify-app.sh`. | `SUFeedURL` is `https://gergoterek.github.io/HoverClick/appcast.xml`; `SUPublicEDKey` is present; `SUEnableAutomaticChecks`, `SUAutomaticallyUpdate`, and `SUAllowsAutomaticUpdates` are false; no raw GitHub latest-release API, git-push update source, placeholder feed URL, or automatic background update behavior is configured. |
| v0.8.0 appcast workflow is published through GitHub Pages | Inspect `docs/APPCAST_RELEASE_WORKFLOW.md`, `docs/UPDATER_DESIGN.md`, GitHub Pages settings, and the live appcast URL. | The feed hosting strategy is a dedicated `gh-pages` branch serving `appcast.xml` at the branch root; GitHub Pages is configured from `gh-pages` `/`; the live appcast contains version `0.8.0` / build `37`, points to `https://github.com/gergoterek/HoverClick/releases/download/v0.8.0/HoverClick-0.8.0.dmg`, and includes a Sparkle EdDSA enclosure signature. |
| v0.8.0 appcast helper is non-publishing by default | Run `scripts/prepare-appcast.sh --help`, then inspect the script. | The helper requires a future real DMG path, public GitHub Release asset URL, version, build, and output path; dry-run is the default; `--write` is required before any appcast is written; the script does not package a DMG, create a tag, create a GitHub Release, upload assets, publish Pages output, or accept private key files. |
| v0.8.0 appcast release gates are documented | Inspect `docs/APPCAST_RELEASE_WORKFLOW.md`. | The future workflow stops if the Sparkle Keychain key is unavailable, Pages hosting is unknown, the live appcast URL is unreachable, the appcast points to the wrong DMG, the signature does not match, duplicate/wrong release assets exist, version/build mismatch occurs, or signing identity, bundle ID, or app name changed unexpectedly. |
| v0.9.0 updater-completion release is documented | Inspect `docs/V0.9.0_UPDATER_COMPLETION_PLAN.md`, `docs/CURRENT_STATE.md`, `docs/UPDATER_DESIGN.md`, `docs/APPCAST_RELEASE_WORKFLOW.md`, and `docs/WORKFLOW.md`. | v0.9.0 is documented as `0.9.0` / build `38` and scoped as `Complete Updater & 1.0 Readiness`; manual `Check Now...` remains; optional automatic checks are user-consented and reversible; silent/background install remains disabled; GitHub Release DMG `HoverClick-0.9.0.dmg` plus GitHub Pages appcast is the historical release path; no app identity, signing identity, bundle ID, or Sparkle key change was planned. |
| v0.9.0 automatic-check UX remains conservative | Inspect `Info.plist`, `HoverClick.mm`, and the built app with `scripts/verify-app.sh`. Manual Finder UI validation -- not run automatically. Open the status menu after `scripts/run-app.sh` and inspect the update items. | The app keeps `SUEnableAutomaticChecks = false`, `SUAutomaticallyUpdate = false`, and `SUAllowsAutomaticUpdates = false`. `Check Now...` remains available. `Automatically Check for Updates` appears as a user-controlled toggle backed by Sparkle's `automaticallyChecksForUpdates`; it can be turned on and off without enabling silent/background install. |
| v0.9.0 appcast workflow rejects wrong assets | Inspect `docs/APPCAST_RELEASE_WORKFLOW.md` and run safe appcast dry-runs only when a real public DMG path and final public GitHub Release URL exist. | The workflow requires `HoverClick-<version>.dmg`, final GitHub Release URL, expected version/build, correct length, and Sparkle signature. It stops for private key material, local paths, placeholder URLs, internal DMGs, duplicate assets, or non-final release URLs. |
| v0.9.0 Hover Click Assist is removed from the current product surface | Inspect `HoverClick.mm`, `docs/V0.9.0_UPDATER_COMPLETION_PLAN.md`, menu docs, and diagnostics docs. Manual Finder UI validation -- not run automatically. | v0.9.0 does not implement real Hover Click Assist, Click-Time Hover Assist, hover/event semantics, mouse-move focus, synthetic clicks, event replay, cursor movement, delayed click delivery, scroll focus, or key focus. The visible `Hover` submenu and `Hover Click Assist` placeholder are removed, and copied diagnostics no longer report a no-op Hover Click Assist setting/runtime behavior. |
| v1.0 updater-readiness gates are explicit | Inspect `docs/V0.9.0_UPDATER_COMPLETION_PLAN.md`. | Before v1.0, updater checks work from an older updater-enabled build, appcast workflow is repeatable, no private key material leaks, release asset naming is stable, validation checklist exists, Accessibility onboarding and runtime revocation remain stable, left/right click focus still work, diagnostics are clear, no misleading no-op Hover Assist remains, and no duplicate/internal DMG assets are part of release publication. |
| v1.0 readiness plan is complete | Inspect `docs/V1.0_READINESS_PLAN.md`, `docs/CURRENT_STATE.md`, and `docs/WORKFLOW.md`. | v1.0 is framed as a conservative polish/readiness release; release prep targets v1.0.0 / build 39 from main/origin-main `446478237ca32cfb1a4316f9f6f2931c6e56946f`; blockers, manual validation, docs/GitHub polish, updater validation, Accessibility validation, diagnostics sanity, branch cleanup planning, deferred risky features, next branch, model level, and release-scope boundary are recorded without runtime, signing, package, appcast, or event/input changes. |
| v1.0 readiness is not release work | Inspect docs before any v1.0 release task. | The readiness polish task does not bump version/build, create a tag, create a GitHub Release, run `scripts/package-dmg.sh`, publish or modify `appcast.xml`, upload release assets, or start release packaging. Release work requires explicit user scope confirmation after review and any needed manual validation. |
| v1.0 updater settings remain conservative | Inspect `Info.plist`, `docs/UPDATER_DESIGN.md`, `docs/APPCAST_RELEASE_WORKFLOW.md`, and copied diagnostics after manual validation. | `SUFeedURL` remains `https://gergoterek.github.io/HoverClick/appcast.xml`; `SUPublicEDKey` remains `093ZOOvjGmr8WkI31IzBnjGwM3GXZU1q/qgDgADWm9o=`; automatic checks default off; manual `Check Now...` remains; automatic download/install remains disabled; appcast publishing is release workflow only, not readiness polish. |
| v1.0.0 release metadata is prepared | Inspect `Info.plist`, README, `docs/CURRENT_STATE.md`, `docs/APPCAST_RELEASE_WORKFLOW.md`, `docs/WORKFLOW.md`, and `docs/ARCHITECTURE.md`. | Version/build are `1.0.0` / `39`; the target public DMG is `HoverClick-1.0.0.dmg`; the target release URL is `https://github.com/gergoterek/HoverClick/releases/tag/v1.0.0`; the appcast item target is version `1.0.0` / build `39`; and app name, bundle identifier, signing identity, Sparkle feed URL, Sparkle public key, updater policy, event tap mask, and mouse semantics remain unchanged. |
| v1.1.x planning doc is complete | Inspect `docs/V1.1_PLANNING.md`, `docs/CURRENT_STATE.md`, `docs/WORKFLOW.md`, and this matrix. | The v1.1.x plan records the v1.0.0 / build 39 baseline, expected main/origin-main baseline `711a76d810f993b8c1c8e57730fd57c26476988d`, live v1.0.0 appcast state, unchanged event tap/mouse semantics, v1.1.x intent, updater completion, menu UI/UX refactor, README reduction, suggested branch/release sequence, non-goals, manual validation expectations, and safety constraints. |
| v1.1.x planning remains docs-only | Inspect the branch diff before merge. | Only `docs/V1.1_PLANNING.md`, `docs/CURRENT_STATE.md`, `docs/WORKFLOW.md`, and `docs/TEST_MATRIX.md` are changed. `README.md`, `HoverClick.mm`, `Info.plist`, scripts, `appcast.xml`, release assets, DMGs, Sparkle keys, signing settings, bundle identity, version/build metadata, event tap mask, and mouse semantics are unchanged. |
| v1.1.x updater completion scope is conservative | Inspect `docs/V1.1_PLANNING.md` before an updater implementation branch. | The plan keeps manual `Check Now...`, keeps `Automatically Check for Updates` clear and persistent, requires diagnostics to show updater state, preserves GitHub Release DMG plus `gh-pages` appcast workflow, forbids private Sparkle key exposure, and does not allow silent/background automatic install without explicit approval. |
| v1.1.x menu UI/UX scope avoids event semantics | Inspect `docs/V1.1_PLANNING.md` before a menu implementation branch. Manual Finder UI validation -- not run automatically. | The plan targets clean section headers, grouped items, clean spacing, native macOS feel, less clutter, and the current Functions, Access, Info, Updates, and bottom About/Quit menu structure. It is explicitly menu polish, not a click/event semantics change. |
| v1.1.x README reduction preserves useful detail | Inspect `docs/V1.1_PLANNING.md` before a README implementation branch. | The plan reduces README to roughly one half or one third of its current length, keeps concise public product/install/update/permissions/troubleshooting entry points, and moves technical detail into docs instead of deleting useful material. |
| v1.1 updater diagnostics are complete | Build and verify the signed app, then use manual Finder UI validation -- not run automatically. Copy diagnostics after launch and after toggling `Auto Check Updates` on and off. | Diagnostics report manual update check availability, appcast URL, Sparkle public key presence, automatic-check default/current state, last automatic-check toggle, automatic download/install default/current state, automatic-install allowed default/current state, and a clear safety note that automatic checks do not enable silent/background install. No private key material appears. |
| v1.1 automatic update checks remain notification-only | Manual Finder UI validation -- not run automatically. Toggle `Auto Check Updates`, relaunch through the signed `.app` workflow when intentionally validating UI, and inspect the menu plus copied diagnostics. | The right-side state indicator reflects the persisted Sparkle automatic-check state. `automaticallyDownloadsUpdates` remains disabled, automatic install remains not allowed, and the menu does not expose or enable silent/background automatic install. |
| v1.1 updater completion preserves event semantics | Inspect `HoverClick.mm`, run `scripts/ci-safety-check.sh`, and perform normal manual focus validation only when intended. | The event tap mask remains left mouse down + right mouse down only; normal events return the original event unchanged; no mouse moved, mouse dragged, mouse up, scroll, synthetic click, event replay, delayed click delivery, cursor movement, Hover Click Assist UI, signing, appcast, release asset, or `Info.plist` change is introduced. |
| v1.1 menu sections are clear | Manual Finder UI validation -- not run automatically. Open the HoverClick status menu after `scripts/run-app.sh`. | The menu opens normally and shows a green status dot, `HoverClick is running`, right-aligned `v<short-version>`, Functions, Access, Info, Updates, and bottom-separated `About` plus `Quit`. No General, Click Focus, top-level Diagnostics, or Quit section header remains. |
| v1.1 menu action rows use native icons | Manual Finder UI validation -- not run automatically. Inspect top-level and submenu action rows. | Menu rows use aligned native `NSMenuItem.image` icons where SF Symbols are available. No manual title-padding, emoji/text icon prefix, or messy `icon + space + label` appearance is used. Custom toggle/status rows use right-side indicators for state. |
| v1.1 menu non-closing controls stay open | Manual Finder UI validation -- not run automatically. Open the HoverClick status menu and click Left Click Focus, Right Click Focus, Launch at Login, Refresh Status, Copy Summary, Verbose Mode, and Auto Check Updates. | Each listed row performs its action without dismissing the menu, and any enabled/checked state updates in place with a right-side indicator. External/opening actions, About, Check for Updates, Accessibility Settings, and Quit may close normally. |
| v1.1 menu controls remain functional | Manual Finder UI validation -- not run automatically. Toggle `Left Click Focus`, `Right Click Focus`, `Launch at Login`, `Verbose Mode`, and `Auto Check Updates`; use permission refresh, diagnostics copy, update check, Help actions, About, and Quit as appropriate. | All controls remain present, easy to find, enabled where expected, and function as before. Help links require explicit user clicks, Uninstall only shows instructions, updater completion diagnostics remain intact, automatic download/install remains disabled, and no Hover Click Assist UI appears. |
| v1.1 menu refactor preserves click behavior | Manual Finder UI validation -- not run automatically. After menu inspection, test mouse movement, left-click focus, right-click focus, active-window right-click, double-click, drag, and menu-open behavior. | Mouse movement alone does not focus windows; click-focus behavior is unchanged; active-window context menus, double-click, drag, and normal clicks remain native and not delayed; opening the menu does not focus windows behind it. |
| v0.8.0 Accessibility onboarding appears on first launch | Manual Finder UI validation -- not run automatically. In a safe fresh-user or fresh-install scenario with Accessibility not yet granted, launch only the signed `HoverClick.app` bundle. | HoverClick requests the native Accessibility trust prompt with `AXIsProcessTrustedWithOptions`, shows an explanatory native alert, keeps the app open, does not open System Settings automatically, and leaves normal click events unchanged. |
| v0.9.0 missing Accessibility disables focus controls | Manual Finder UI validation -- not run automatically. Launch without Accessibility permission and open the status menu. | `Access` > `Permissions` shows `Accessibility: Required`; `Left Click Focus` and `Right Click Focus` are disabled until permission is granted; saved checked states are preserved. Update actions remain separate from Accessibility-gated click-focus controls. |
| v0.8.0 permission state refreshes on activation and menu open | Manual Finder UI validation -- not run automatically. Change Accessibility permission state manually, return to HoverClick, then open the HoverClick status menu. | HoverClick refreshes trust without prompting when the app becomes active and before the status menu opens; menu state, feature enabled state, diagnostics, and event tap lifecycle reflect the current permission state without requiring relaunch. |
| v0.8.0 permission refresh dismisses onboarding | Manual Finder UI validation -- not run automatically. Grant Accessibility through the normal System Settings flow while the onboarding alert is visible if practical, then return to HoverClick and choose `Access` > `Permissions` > `Refresh Status`. | The onboarding alert closes if it is still visible, status changes to `Accessibility: Granted`, click-focus controls become enabled again, and the event tap installs when the user still wants click detection. |
| v0.8.0 permission refresh avoids duplicate onboarding UI | Manual Finder UI validation -- not run automatically. With Accessibility still missing, choose `Refresh Status` repeatedly. | HoverClick may request the native trust prompt for the explicit action, but it does not stack duplicate HoverClick onboarding alerts or create a modal loop; feature controls remain disabled and permission actions remain available. |
| v0.8.0 runtime Accessibility revocation fails open | Manual Finder UI validation -- not run automatically. With HoverClick running and Accessibility granted, manually revoke Accessibility from System Settings, then click normally in other apps. | Normal clicks remain usable immediately. If a left/right mouse-down callback still arrives through a stale tap, HoverClick returns the original event unchanged, records permission-missing pass-through, schedules stale tap removal, performs no AX focus work, and does not show repeated modal alerts. |
| v0.8.0 runtime revocation recovers with permission refresh | Manual Finder UI validation -- not run automatically. After runtime revocation, grant Accessibility again through the normal System Settings flow and choose `Access` > `Permissions` > `Refresh Status`. | The menu changes back to `Accessibility: Granted`, feature controls re-enable, the event tap reinstalls without requiring relaunch when possible, and diagnostics show the latest permission refresh/check result. |
| v0.8.0 Launch at Login onboarding asks once | Manual Finder UI validation -- not run automatically. In a fresh/default configuration on macOS 13 or newer with the main app login item not registered, launch the signed app. | HoverClick asks whether to enable Launch at Login, records `launchAtLoginOnboardingPromptShown`, registers only if the user chooses `Enable Launch at Login`, and does not silently enable startup or change Accessibility permission. |
| v0.8.0 onboarding diagnostics are copied | Manual Finder UI validation -- not run automatically. Use `Info` > `Diagnostics` > `Copy Summary` before and after granting Accessibility or after a runtime revocation test. | Diagnostics include Accessibility permission, Accessibility onboarding shown this launch, Accessibility trust prompt requested this launch, whether click focus is disabled because permission is missing, permission-missing pass-through active, event tap removed due to missing permission, event tap callback pass-through count, last permission refresh/check result, Launch at Login status, and Launch at Login onboarding decision. |
| v0.7.0 release-readiness focus tests | Manual Finder UI validation -- not run automatically. Exercise left-click background focus, right-click background focus, Finder background focus, Chrome background focus, and PDF Expert background focus if PDF Expert is available. | Focus attempts start for normal eligible background windows, the original click events pass through unchanged, right-click context menus remain native, and diagnostics show target detection, `focusAttempt=yes`, AX operation details, immediate verification, delayed verification when needed, and final success or a precise failure reason. |
| v0.7.0 Window Server pass-through diagnostics | Manual Finder UI validation -- not run automatically. Repeat a normal app-window click that previously reported a tiny untitled no-bundle `Window Server` high-layer surface near the click point. | Diagnostics include `Window Server pass-through surface ignored`, `focusAttempt=yes`, and delayed verification passed when immediate verification needs the delayed check. Menu/status UI skip remains protected. |
| App icon source and bundle resource are present | Inspect `assets/HoverClickAppIcon-1024.png`, `Resources/HoverClick.icns`, `Info.plist`, and the built app after `scripts/build-app.sh`. | The source PNG exists at `assets/HoverClickAppIcon-1024.png`; generated `.icns` exists at `Resources/HoverClick.icns`; `CFBundleIconFile` is `HoverClick.icns`; the built bundle contains `HoverClick.app/Contents/Resources/HoverClick.icns`; verification checks pass. |
| DMG volume icon source and resource are present | Inspect `assets/HoverClickDMGVolumeIcon.png`, `Resources/HoverClickDMGVolumeIcon.icns`, and `scripts/package-dmg.sh`. | The dedicated DMG volume icon PNG exists at `assets/HoverClickDMGVolumeIcon.png`; generated `.icns` exists at `Resources/HoverClickDMGVolumeIcon.icns`; `scripts/package-dmg.sh` uses the DMG icon resource for `.VolumeIcon.icns`; the app icon remains `Resources/HoverClick.icns`. |
| App icon smoke test | Manual Finder UI validation -- not run automatically. Build with `scripts/build-app.sh`, verify with `scripts/verify-app.sh`, then use `scripts/run-app.sh` as the approved signed `.app` runtime refresh before inspecting normal macOS icon surfaces. | HoverClick shows the branded app icon; the icon is not a placeholder, stock icon, missing generic app icon, clipped, or visibly distorted. |
| Internal DMG presentation assets verify from command line | Run `scripts/package-dmg.sh`. The script builds, verifies, packages, mounts with `hdiutil -nobrowse -noautoopen`, and detaches without opening Finder. | The generated DMG contains the expected `HoverClick.app`, bundle identifier `com.gergoterek.HoverClick`, matching version/build, valid code signature, `Applications` symlink to `/Applications`, `.VolumeIcon.icns` sourced from `Resources/HoverClickDMGVolumeIcon.icns`, hidden volume icon file flag, and custom volume icon flag. |
| Internal DMG Finder presentation smoke test | Manual Finder UI validation -- not run automatically. After `scripts/package-dmg.sh`, mount the generated DMG manually only when a Finder presentation smoke test is intended. | The mounted DMG volume shows the branded volume icon, `HoverClick.app` is visible, the `Applications` shortcut is available for drag-install, and any future Finder background/layout polish is either verified manually or explicitly documented as not automated. If Finder is configured to show hidden files, `.VolumeIcon.icns` may be visible even though its hidden flag is set. Runtime/event-tap behavior is unchanged. |
| DMG file icon expectation is documented | Inspect docs before release packaging. | The `.dmg` file's own pre-mount Finder icon is not treated as a release feature because custom file icons depend on local resource-fork or extended-attribute metadata that is not a reliable GitHub Release asset property after upload/download; the supported icon polish is the mounted DMG volume icon. |
| App launches as `.app` | Manual Finder UI validation — not run automatically. Run `/Users/gergoterek/Movies/OBS/GPT/HoverClick/scripts/run-app.sh` only when a manual UI test is intended. | The app is opened through `HoverClick.app`, not the raw binary. |
| v1.0 final manual smoke checklist | Manual Finder UI validation -- not run automatically. Use only when the user intentionally runs final readiness validation. | The signed `.app` launches; Accessibility permission remains stable; the menu opens and menu items work; moving the mouse over background windows does not focus them; left-click focus works; right-click focus works when enabled and context menus remain normal; `Check Now...` works without a 404; `Automatically Check for Updates` persists; no silent/background automatic install occurs; diagnostics copy works and reports updater automatic-check/download/install state; no Hover Click Assist UI remains visible; event semantics are unchanged. |
| Exactly one instance runs | Manual Finder UI validation — not run automatically. Run `scripts/run-app.sh` only when a manual UI test is intended, then run `scripts/verify-app.sh`. | Verification reports exactly one HoverClick process. |
| Menubar icon is clear and balanced | Manual Finder UI validation — not run automatically. Compare the HoverClick menu bar status item with nearby native status items after relaunch. | The `cursorarrow.click` template icon is slightly smaller than the previous 17 pt version, still clearer than the original tiny icon, vertically aligned, and not clipped, blurry, or stretched. |
| Menubar icon remains native template | Inspect source and check the current macOS appearance manually. | The icon uses a template SF Symbol with native AppKit tinting and looks correct in the current light/dark menu bar appearance. |
| Menu appears | Manual Finder UI validation -- not run automatically. Check the macOS menu bar. | A HoverClick status item appears with a running status row, Functions containing click-focus toggles, Access containing the `Permissions` submenu, Info containing `Help` and `Diagnostics`, a separate Updates section, and bottom-separated `About` plus `Quit`. |
| Header row appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | A green dot, `HoverClick is running`, and right-aligned `v1.0.0` appear on one compact grey, non-clickable header row; the visible version is generated from `CFBundleShortVersionString`, and no build number appears in the header. |
| Header tooltip is release-independent | Manual Finder UI validation — not run automatically. Hover over the header row and status item. | Tooltip text, if shown by macOS, is stable and release-independent: `HoverClick - Windows-like click focus for macOS.` It does not mention maintenance, updates, releases, `UI-Menubar`, permissions layout, hover submenu, or live version display. |
| About version follows Info.plist | Inspect `Info.plist`, build, relaunch through `scripts/run-app.sh`, then open About manually. | The header shows right-aligned `v<CFBundleShortVersionString>` and bottom `About` shows `Version <CFBundleShortVersionString>` plus `Build <CFBundleVersion>` from built app metadata; the status-item tooltip and Diagnostics submenu do not show version/build. |
| Permissions submenu appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Access` > `Permissions` appears as a submenu containing Accessibility status, Launch at Login, separator, Refresh Status, and Accessibility Settings at the bottom. |
| Diagnostics submenu appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Info` > `Diagnostics` appears as a submenu containing `Copy Summary` and `Verbose Mode` without version/build, persistent Event Tap, Last Click, Last Action, Click Detection, or Event Tap detail rows. |
| About item appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu, then choose bottom `About`. | A small native informational alert shows `HoverClick`, `Version 1.0.0`, `Build 39`, `Bundle ID: com.gergoterek.HoverClick`, and `Windows-like click focus for macOS.` The row has no icon. |
| Hover Click Assist placeholder is absent | Manual Finder UI validation -- not run automatically. Open the HoverClick status menu. | No `Hover` submenu and no `Hover Click Assist` item appear in the current v1.0.0 product surface. |
| Menu visual columns align | Manual Finder UI validation — not run automatically. Open the HoverClick status menu and submenus. | `About` and `Quit` act as the plain-row visual anchor. The top status row, smaller section-header labels, submenu titles, icon-bearing rows, right-side toggle/status indicators, submenu arrows, the header version label, and the `Quit` Cmd+Q shortcut align into consistent left and right columns as closely as native AppKit permits. Action rows use native menu item images without text icon prefixes, emoji prefixes, or leading title padding. Toggle/status rows show state on the right, not as a far-left native checkmark. Any remaining shared gutter is native AppKit menu layout. |
| Toggle titles are stable | Manual Finder UI validation -- not run automatically. Open the HoverClick status menu. | `Left Click Focus`, `Right Click Focus`, `Auto Check Updates`, and `Launch at Login` do not include `On` or `Off` in their titles. |
| Toggle state uses right-side indicators | Manual Finder UI validation — not run automatically. Open the HoverClick status menu before and after toggling feature items. | Enabled features show a right-side indicator; disabled features are unchecked; Launch at Login may use a right-side mixed indicator if macOS reports approval is required. |
| Menu item help text is available | Manual Finder UI validation — not run automatically. Hover over relevant status menu items. | Parent submenu items do not show tooltips; specific native menu tooltip/help text appears for relevant actionable and status child items if macOS displays menu item tooltips. |
| Accessibility status is detected correctly | Use the status menu permission item before and after granting permission. | The submenu item reports `Accessibility: Granted` or `Accessibility: Required`; `Accessibility: Granted` shows a right-side state indicator only when permission is granted, and logs report trusted `YES` or `NO`. |
| Signing identity is stable | Run `scripts/build-app.sh`, then `scripts/verify-app.sh`. | Codesign authority includes `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`. |
| Rebuild does not change bundle identifier | Run `scripts/build-app.sh` repeatedly and verify `CFBundleIdentifier`. | Bundle identifier remains `com.gergoterek.HoverClick`. |
| Raw binary is never used | Inspect scripts and build commands. | Scripts launch only `HoverClick.app`; no script runs `Contents/MacOS/HoverClick` directly. |
| Event tap installs when allowed | Manual Finder UI validation — not run automatically. Grant Accessibility, then run `scripts/run-app.sh` only when a manual UI test is intended. | Logs include `[HoverClick] event tap installed mode=pass-through-default`; click detection status is available through `Copy Summary`. |
| Missing Accessibility is handled calmly | Remove or do not grant Accessibility, then launch the app. | The app remains open, menu status shows `Accessibility: Required`, feature toggles are disabled, and System Settings does not open unless the user explicitly chooses `Accessibility Settings`. |
| Fresh Accessibility permission flow is understandable | Manual release-readiness validation only. Use a safe clean-user or fresh manual-install scenario when available; do not run this during normal development. Launch only the signed `HoverClick.app` bundle, then grant permission through the normal System Settings flow if prompted. | The app keeps bundle identifier `com.gergoterek.HoverClick`, remains signed by `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`, never uses a raw binary launch, shows understandable Accessibility prompt/status behavior, allows the user to grant Accessibility normally, opens the menu after permission is granted, `Refresh Status` refreshes the trusted state, Left Click Focus works, copied diagnostics show expected Accessibility and event tap state, and a normal quit/relaunch does not unexpectedly re-prompt. Do not use `tccutil reset`; only document risky permission-reset validation separately if the user explicitly chooses it. |
| Left mouse down is observable | With Accessibility granted and click detection active, click anywhere while watching the unified log. | Logs include `[HoverClick] click #... received leftClickFocus=... raw=(x,y) converted=(x,y)`. This is event detection only. |
| Technical click detection is not a persistent row | Manual Finder UI validation — not run automatically. Open the status menu and submenus. | `Event Tap` / `Click Detection` is not shown as a separate persistent menu row. |
| Last action is not a persistent row | Manual Finder UI validation — not run automatically. Open the status menu and submenus. | `Last Click` / `Last Action` is not shown as a separate persistent menu row. |
| Copy Summary works | Manual Finder UI validation -- not run automatically. Click `Info` > `Diagnostics` > `Copy Summary`, then paste into a text field. | The copied text includes app name, bundle identifier, Accessibility, Accessibility onboarding/trust-prompt state, permission-gated click-focus state, permission-missing pass-through/removal state, last permission refresh/check result, Launch at Login, Launch at Login onboarding decision, automatic update checks, automatic download/install state, click detection, last handled action, preserved last focus action/skip, last non-menu focus action/skip, last focus decision detail, last right-click focus decision, stable last real/background click decision fields, overlay/system UI skip detail, last eligible hit-test candidate, recent non-menu mouse-down decision history, aggregate callback/focus/skip counters, persistent background-focus trigger/target/frontmost-before/activation/AX/immediate/delayed/final verification/failure/success fields, feature states, expanded event tap lifecycle fields, last left/right mouse-down seen, event tap mask, safety note, and concise known limitations; it does not include Hover Click Assist no-op setting/runtime rows; version/build are available from bottom `About` instead of duplicated here. |
| Event tap lifecycle diagnostics are complete | Manual Finder UI validation — not run automatically. Copy diagnostics after launch and again after at least one left and right mouse-down. | Diagnostics include event tap requested state, object/source presence, port/source validity, believed installed/enabled state, detected enabled state when available, last event tap callback, last left/right mouse-down timestamps, last tap recovery attempt/result, last focus decision detail, last right-click focus decision, persistent last background-focus execution fields, and last verified successful background focus. |
| Background focus diagnostics persist across menu clicks | Manual Finder UI validation — not run automatically. Focus a background window with left-click or right-click focus, then open the HoverClick menu and copy diagnostics. | `Last handled action` may reflect a menu/overlay click, but `Last non-menu focus action/skip`, `Last real/background click decision`, recent non-menu mouse-down decision history, `Last background focus attempt`, execution details, result, verification, delayed verification, and `Last verified successful background focus` still describe the most recent meaningful click path instead of being erased by the menu interaction. |
| Duplicate event tap install is skipped | Click Accessibility status refresh while click detection is already active. | Logs include `event tap already installed; skipping duplicate install` and only one HoverClick process remains. |
| Event tap timeout disabled path | Manual stress validation only. If macOS disables the tap by timeout, watch logs and copy diagnostics. | Logs include `event tap disabled by timeout`; when user intent is still enabled, recovery either logs `event tap re-enabled after timeout` for a valid existing tap or logs recreation when the port/source is invalid or re-enable fails. Diagnostics show the last callback and recovery result. |
| Event tap user-input disabled path | Manual system validation only. If macOS disables the tap by user input, watch logs and copy diagnostics. | Logs include `event tap disabled by user input`; when user intent is still enabled, recovery either re-enables the existing tap or recreates the left/right mouse-down-only tap. Diagnostics show the actual/believed tap state and recovery result. |
| Long-run click focus remains available | Manual long-run validation only. Launch with `scripts/run-app.sh`, confirm left and right click focus work, leave HoverClick running through extended idle and sleep/wake or lock-unlock if practical, then test left and right click focus again. | Left and right click focus continue to work. If callbacks still arrive but focus does not happen, copied diagnostics immediately show whether each recent non-menu mouse-down reached target resolution and policy, whether an eligible candidate was found, the exact skip reason, whether overlay/system UI or compact-popup classification was involved, whether a focus attempt started, AX operation summary, immediate and delayed verification, final result, callback/focus/skip counters, and stable last real/background click fields that are not overwritten by copying diagnostics. |
| Launch at Login menu item appears | Manual Finder UI validation — not run automatically. Open `Access` > `Permissions`. | `Launch at Login` appears inside the submenu as an independent checked/unchecked menu item. |
| Launch at Login toggles on | Manual Finder UI validation — not run automatically. Click `Launch at Login`, close the menu, then reopen it. | The item remains checked when ServiceManagement reports the main app login item is enabled. |
| Launch at Login toggles off | Manual Finder UI validation — not run automatically. Click `Launch at Login` again, close the menu, then reopen it. | The item remains unchecked when ServiceManagement reports the main app login item is not registered. |
| Launch at Login requires approval | If macOS reports the login item requires user approval, inspect the menu/logs. | The menu uses a mixed state and logs `requires approval` without opening System Settings automatically. |
| Accessibility Settings is explicit | Manual Finder UI validation — not run automatically. Open `Access` > `Permissions` without clicking the settings item. | System Settings does not open unless `Accessibility Settings` is clicked explicitly; the item uses a native menu image and no title padding. |
| Quit remains an action | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Quit` appears directly below `About` after a bottom separator, has no icon, remains unchecked, keeps the Cmd+Q shortcut, and has no Quit section header. |
| Mouse movement over background window | Manual Finder UI validation — not run automatically. Move the pointer over a background Finder/Chrome/iTerm window without clicking. | No focus change occurs, and no hover-to-focus logs appear. |
| Mouse movement over current front app | Move the pointer within the current frontmost app. | Focus remains unchanged and no AX focus/raise/activate path runs from mouse movement. |
| Left click focus remains available | With `Left Click Focus` checked, click a visible background Finder/Chrome/iTerm window. | The target window focuses before click delivery and the original click event is passed through unchanged. |
| Right Click Focus defaults off | Launch HoverClick after a fresh/default configuration, then inspect the status menu. | `Right Click Focus` appears as an enabled, unchecked menu item. |
| Right Click Focus off preserves normal right-clicks | With `Right Click Focus` unchecked, right-click a visible background Finder/Chrome/iTerm window. | HoverClick does not run the focus path for the background target, the original right-click event passes through unchanged, and the normal context menu behavior is preserved. Copied diagnostics show the right mouse down was seen and the right-click-specific decision says the event was skipped because Right Click Focus was disabled. |
| Right Click Focus toggles independently | Toggle `Right Click Focus` while `Left Click Focus` stays on, then toggle `Left Click Focus` while `Right Click Focus` stays off or on. | Each setting updates independently; left-click focus remains controlled only by `Left Click Focus`, and right-click focus remains controlled only by `Right Click Focus`. |
| Right Click Focus on focuses background window | With `Right Click Focus` checked, right-click a visible background Finder/Chrome/iTerm window. | The target window focuses before right-click delivery, logs use the `right-click` trigger, the original right-click event passes through unchanged, and the normal context menu can appear. Copied diagnostics show right mouse down seen, target app/window detection, focus attempt, AX operation result, immediate verification and delayed verification if needed, and last verified successful background focus if verification passed. |
| Right Click Focus on preserves active-window context menu | With `Right Click Focus` checked, right-click inside the already-frontmost app. | HoverClick treats the target as already frontmost, returns the original right-click event unchanged, and the context menu opens normally. Copied diagnostics identify the skip as already-frontmost rather than a focus attempt failure. |
| Right Click Focus ineligible-target diagnostics | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, right-click menu bar, status item, system UI, popover, or compact overlay surfaces. | The UI receives the original right-click unchanged. Copied diagnostics distinguish the event tap/right mouse down observation from a skip because the AX target or topmost CoreGraphics window was not an eligible normal app/window target. |
| Right Click Focus context menu regression | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, right-click background and frontmost Finder/Chrome/iTerm windows. | Normal context menus appear where the app would normally show them. HoverClick does not consume, replay, replace, or post the right-click event. |
| Right Click Focus double-click regression | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, double-click normal items in Finder and text/content areas in another app after the target is frontmost. | Double-click behavior remains native. HoverClick does not add mouse-up, mouse-dragged, synthetic click, event replay, or cursor movement behavior. |
| Right Click Focus background drag/selection regression | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked and unchecked, repeat background text selection, item drag, and active-window drag checks. | Existing background drag/selection behavior is preserved as a known regression-test area. The first-drag limitation may still exist; this batch does not attempt to fix it. |
| Finder context-menu follow-up left click | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, right-click a file in a background or already-frontmost Finder window to open its context menu, then left-click another file. | HoverClick does not perform AX hit-testing, focus, raise, or activation work on the first left click after a recent Finder right-click while Finder is already frontmost; the original left click passes through unchanged, the context menu dismisses normally, and the file receives the click or selection as close as possible to native Finder behavior without synthetic click replay. |
| Old persisted Hover Focus setting is ignored | Set or leave any previous `HoverFocusEnabled` user default, then launch HoverClick normally. | The old setting cannot re-enable mouse-move-to-focus behavior and no Hover Focus menu item appears. |
| Old persisted Hover Click Assist setting is ignored | Set or leave any previous `hoverClickAssistEnabled` user default, then launch HoverClick normally. | The old setting cannot re-enable a menu item or any runtime behavior. No `Hover Click Assist` menu item appears, diagnostics do not report a no-op assist state, and no synthetic click, cursor movement, mouse-move, hover, animated-button, or delayed assist path runs. |
| Verbose Mode toggles | Toggle `Verbose Mode` in the Diagnostics submenu. | The right-side state indicator changes, the menu stays open, and logging detail follows the setting. |
| Right-click event tap remains click-only | Inspect source for right-click support. | Right Click Focus uses `kCGEventRightMouseDown`; no mouse-moved, scroll, synthetic click, cursor movement, replacement event, or delayed assist path is added. |
| Event tap mask remains narrow for v0.7.0 | Inspect `HoverClick.mm` and copied diagnostics. | The event tap mask remains left mouse down + right mouse down only; no synthetic clicks, event replay, cursor movement, mouse-move focus, scroll focus, `mouseDragged`/`mouseUp` handling, `CGEventPost`, `CGEventCreateMouseEvent`, `CGDisplayMoveCursorToPoint`, or `CGWarpMouseCursorPosition` were added. |
| Scroll Focus remains unimplemented | Inspect source for `kCGEventScrollWheel`, scroll focus toggles, or scroll focus handlers. | No scroll focus path exists in the current source-first docs or runtime. |
| Modifier Key Focus remains unimplemented | Inspect source and menus for modifier-key or hold-to-focus controls. | Modifier Key Focus / Hold-to-Focus remains a future idea only and no current event-tap path or menu control exists for it. |
| Failed background drag experiment is not reused | Inspect source and docs before any future click-and-drag work. | The failed 35 ms background drag assist / activation-settle approach is documented as not reusable; no background click-and-drag assist, synthetic click, cursor movement, event replay, `mouseDragged`, or `mouseUp` path exists in the current build. |
| Trigger controls stay separate | Inspect menus and source. | The Functions section includes Left Click Focus and Right Click Focus; update checking is a separate Sparkle setting under Updates; technical click detection appears only in the copied diagnostics summary; Scroll Focus and Hover Click Assist are not presented as current features. |
| Background Finder window focuses | Manual Finder UI validation — not run automatically. Click a visible background Finder window. | Logs show target app Finder, target window, `AXRaise`, app activation, immediate verification, delayed verification only if needed, and Finder becomes frontmost. |
| Background Chrome window focuses | Manual Finder UI validation — not run automatically. Click a visible background Chrome window. | Logs show target app Chrome, target window, action attempts, immediate verification, delayed verification only if needed, and Chrome becomes frontmost. |
| Chrome-to-Finder delayed focus verification passes | Manual Finder UI validation -- not run automatically. From Chrome frontmost, use left-click and right-click background focus on Finder and copy diagnostics. | Manual main validation passed for both triggers: activation attempted=yes returnValue=yes, AX operations attempted:success, immediate frontmost remained Chrome, delayed verification passed after 0.20s with Finder frontmost, result success, and verification delayed passed. |
| Background iTerm window focuses | Manual Finder UI validation — not run automatically. Click a visible background iTerm window. | Logs show target app iTerm, target window, `AXRaise`, activation, immediate verification, delayed verification only if needed, and iTerm becomes frontmost. |
| Background text first-drag limitation | Manual Finder UI validation -- not run automatically. With `Left Click Focus` checked, place a text-heavy editor, browser, or text field in the background, then start a click-drag selection from inactive state. | The window should focus and the original mouse events should pass through unchanged. Some apps may still treat the first mouse-down as activation-only, so first-drag text selection may require a second drag unless a future safe non-replay fix is proven. |
| Active text drag still works | Manual Finder UI validation -- not run automatically. With the same text window already frontmost, click-drag text. | Text selection or drag behaves normally; HoverClick does not interfere because the target app is already frontmost. |
| Menu/status clicks are ignored | Click or right-click the menu bar or HoverClick status item. | Logs show the target was ignored as own app, menu, or status UI. |
| Non-layer-0 overlay clicks pass through | Manual Finder UI validation -- not run automatically. Click menu bar, status-item, app popover, or overlay UI shown above a normal window. | HoverClick does not focus or click the window behind protected menu/status/system UI, compact popup-style overlays, or popovers; the original click event passes through unchanged, and diagnostics show the overlay/system UI skip reason plus topmost owner/window/layer/title/bounds and AX role/subrole/app detail. |
| Large transparent non-normal overlays do not suppress normal windows | Manual Finder UI validation -- not run automatically. If diagnostics previously reported `topmost overlay or system UI click ignored` for an ordinary app-window click, repeat that ordinary background-window click. | When AX resolves a normal eligible app-window candidate beneath a large non-normal top window, HoverClick does not skip solely because the CoreGraphics layer is non-zero. Diagnostics show `not skipped` overlay detail, the eligible hit-test candidate, and a background focus attempt for the target app. |
| Tiny Window Server pointer-like surface does not block normal target | Manual Finder UI validation -- not run automatically. Repeat a normal background-window click that previously reported a 35 x 49-ish untitled no-bundle `Window Server` topmost window and `topmost non-normal popup/menu layer is compact and likely interactive`. | If AX resolves a normal app target such as Finder, Chrome, or PDF Expert, diagnostics report `Window Server pass-through surface ignored`, `compactPopup` is pass-through/ignored rather than a blocking skip, `focusAttempt=yes`, AX operations are attempted, and immediate or delayed verification gives the specific result. |
| Bartender expanded icons pass through | Manual Finder UI validation -- not run automatically. With Bartender 6 expanded/overflow menu bar icons visible, click hidden/overflow icons in the expanded row. | The Bartender icon receives the original click normally; HoverClick does not focus or click the window behind the Bartender overlay, and diagnostics identify the protected menu-bar/system UI or compact-overlay skip. |
| Popover and transient menu UI are ignored | Click menu, popover, or transient app UI when visible. | HoverClick passes the event through without stealing focus away from the current app. |
| Current front app gets minimal action | Click the already-front app. | Logs show the target app is already frontmost and the event passes through. |
| Rapid clicking between apps | Click quickly between Finder, Chrome, iTerm, and Terminal. | Logs keep sequence ids distinct; stale delayed verification callbacks do not overwrite a newer background-focus attempt. |
| Target app disappears after click | Manual validation only. Quit or close a target app immediately after click-to-focus. | HoverClick does not crash; delayed verification either reports the current frontmost app as a failed verification or is skipped as stale if a newer attempt has replaced it. |
| No click behavior changes | Use normal clicks while the event tap is enabled. | The event tap returns the original event unchanged after any focus attempt. |
| No drag behavior changes | Drag windows, text selections, or sliders while the event tap is enabled. | Drag behavior remains unchanged. |
| No window movement or resize | Click background windows and drag normally. | HoverClick logs focus/raise/activation only; windows do not move or resize. |
| No synthetic click | Search source and test normal click behavior. | No `CGEventPost` or synthetic mouse event path exists; the physical click is passed through. |
| No duplicate process or tap | Run `scripts/run-app.sh`, then `scripts/verify-app.sh`; refresh Accessibility status from the menu. | Exactly one app process is running, and duplicate event tap installs are skipped. |
| Menu items stay enabled where applicable | Open the status menu before and after toggling permission, diagnostics state, Left Click Focus, and automatic update checks. | Permissions, Help, Diagnostics, Accessibility, Refresh Status, Verbose Mode, Copy Summary, Accessibility Settings, Check for Updates, Auto Check Updates, About, and Quit are not accidentally greyed out. `Left Click Focus` and `Right Click Focus` are enabled only when Accessibility is granted. No Hover Click Assist placeholder appears. |
| Multi-monitor negative coordinates | Manual Finder UI validation — not run automatically. Click windows on secondary displays that use negative coordinates, if available. | Logs show raw and converted coordinates, target lookup succeeds, and no Retina pixel conversion is needed. |
| Multi-monitor menu bar first-click works | Manual Finder UI validation — not run automatically. With two displays connected and "Displays have separate Spaces" enabled (test both with and without "Automatically hide and show the menu bar"), make an app window active on Display A, then click a menu bar item on Display B. | The first click triggers the menu bar action; HoverClick does not focus a background window on Display B before the click. Copy Diagnostics Summary shows `Last ultra-fast menu-bar passthrough: left #N at TIME raw=(x,y) returned original event unchanged` and `Last menu-bar/top-strip check: ... result=YES-menu-bar bypassed=YES`. The ultra-fast path returns the event before any NSWorkspace IPC, NSDictionary allocation, or diagnostic string formatting — no `beginRecentDecisionWithTrigger:` runs. Repeat in reverse direction (active app on Display B, menu bar click on Display A). Both directions must pass. |
| Multi-monitor menu bar bypass does not affect normal background focus | Manual Finder UI validation — not run automatically. With two displays connected, click a visible background app window (not in the menu bar area) on the secondary display. | Normal background-window focus still occurs; `Last menu-bar/top-strip check:` in Copy Diagnostics Summary shows `result=NO-not-menu-bar`; the full focus path runs (beginRecentDecision, AX, focus attempt, verification); diagnostics show eligible candidate, focus attempt, and verification result as expected. |
| Focus result is not a false positive | Review logs after a background-window click. | Success is based on target resolution plus action and immediate or delayed frontmost verification logs, not only event observation. |

## Chrome / Google Docs Background Click-Through Investigation

Manual Finder UI validation -- not run automatically.

| Test | Method | Expected Result |
| --- | --- | --- |
| Google Docs document body first click | With Finder frontmost and Left Click Focus enabled, click the document body in a background Chrome Google Docs window, then immediately copy diagnostics. | Diagnostics distinguish whether the event tap saw the left mouse-down, Finder was the source/frontmost app before click, Chrome was the target bundle `com.google.Chrome`, target/window AX details resolved, focus attempt started, activation and AX operations were attempted, immediate or delayed frontmost verification passed or failed, the original event was returned unchanged, and whether the remaining miss looks app/web-content-level. |
| Google Docs document body second click | Without changing the target, click the same Google Docs body target a second time and copy diagnostics again. | Compare with the first snapshot. If the first snapshot shows successful focus/pass-through and the second click is handled by Google Docs, the difference supports a Chrome/Docs readiness or web-content click-handling interpretation rather than a HoverClick event-tap failure. |
| Google Docs toolbar or button first click | With Finder frontmost, click a visible Google Docs toolbar control in background Chrome and copy diagnostics. | Diagnostics identify whether the target was Chrome, whether AX suggests toolbar/web content, whether the click was skipped by overlay/menu/system classification, whether focus verification passed, and whether the original event passed through unchanged. |
| Normal website link or button first click | With Finder frontmost, click a normal website link or button in background Chrome and copy diagnostics. | Diagnostics show whether the behavior is general Chrome/web-content click handling or specific to Google Docs. A focus-success plus missed-click result points outside HoverClick's directly observable focus path. |
| Chrome toolbar or address bar first click | With Finder frontmost, click Chrome toolbar UI or the address bar in background Chrome and copy diagnostics. | Diagnostics show Chrome target bundle/window details and whether AX role/title differs from document web content. A successful toolbar click while Google Docs content misses narrows the issue toward web-app content handling. |
| Background Chrome non-Google Docs page | With Finder frontmost, click a background Chrome page that is not Google Docs and copy diagnostics. | Diagnostics distinguish Chrome-general behavior from Google Docs-specific behavior using target bundle, AX/window hints, focus verification, pass-through, and final click-through investigation text. |
| Left Click Focus off comparison | Turn Left Click Focus off, repeat a Finder-to-Chrome click if useful, then copy diagnostics. | HoverClick should record that Left Click Focus was disabled, perform no target focus attempt, and pass the original event unchanged. This separates native Chrome inactive-window behavior from HoverClick's focus path. |
| Right Click Focus comparisons | With Right Click Focus off and on, repeat safe background Chrome right-click checks if useful. | Right-click diagnostics should remain independent from left-click behavior and should continue to show observation, disabled-setting skips or focus attempts, AX results, verification, and pass-through. |
| Failure classification | Review first-click and second-click diagnostics side by side. | The snapshots should classify the miss as one of: focus failure, focus success but app/web-content click not handled, overlay/menu/system UI skip, stale permission fail-open, Chrome/Google Docs-specific behavior, or general browser behavior. |

## Stable Checkpoint Manual Validation

Manual Finder UI validation -- not run automatically.

The current stable checkpoint has been manually validated with:

- Left Click Focus ON focuses background windows and returns the original left-click event unchanged.
- Left Click Focus OFF passes left-clicks through without running the focus path.
- Right Click Focus ON focuses the target background window first and returns the original right-click event unchanged so the normal context menu works.
- Right Click Focus OFF passes right-clicks through without running the focus path.
- Finder context-menu follow-up left-click passes through without AX hit-testing after a recent Finder right-click while Finder is frontmost.
- Bartender/menu-bar overlay clicks pass through without focusing the window behind the overlay.
- The HoverClick status menu, Functions section, Access > Permissions submenu, Info > Help/Diagnostics submenus, Updates section, Copy Summary, Accessibility Settings action, bottom About item, and Quit/Cmd+Q remain present and uncluttered.
- Launch at Login works.
- Mouse movement alone does not focus windows.
- Known limitations are documented as limitations, not failures: Finder right-click actual selection may not change, and background text first-drag can require a second drag in some apps.
- Accessibility permission did not reappear.
- No synthetic click, cursor movement, `CGEventPost`, `CGDisplayMoveCursorToPoint`, `kCGEventMouseMoved`, or scroll-focus behavior exists.
- For the closed v0.4.6 release on main, the overlay/system UI skip ordering fix was manually validated: a large transparent/non-interactive non-layer-0 window did not suppress a normal eligible Chrome app/window target, menu/status/Bartender overlay protection remained intact, last non-menu focus action/skip and eligible hit-test candidate diagnostics were preserved, activation attempted=yes returnValue=yes, AX operations attempted:success, delayed verification passed after 0.20s, result success, and verification delayed passed.
- The published v0.4.6 DMG smoke test passed with SHA-256 `4e31b9196458e326bc794dbeb33525ce4a8d2b58fe463de0e9c3c789d3a6c076`, and the user confirmed the installed app worked normally.
