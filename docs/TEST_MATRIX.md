# Test Matrix

| Test | Method | Expected Result |
| --- | --- | --- |
| Release test plan is completed | Follow `docs/RELEASE_TEST.md` before tagging or publishing a source-first release candidate. | Manual release, Accessibility, focus, menu, diagnostics, Launch at Login, internal DMG, and performance sanity checks are complete or documented with clear exceptions. |
| Published v0.4.6 GitHub DMG smoke test passed | Review the recorded post-release result for the downloaded GitHub release asset `HoverClick-0.4.6.dmg`. | SHA-256 matched `4e31b9196458e326bc794dbeb33525ce4a8d2b58fe463de0e9c3c789d3a6c076`; launch worked; menu opened; version was `0.4.6` / build `32`; Accessibility status was correct; Left Click Focus worked; Right Click Focus worked when enabled; diagnostics background-focus fields worked; Finder context-menu follow-up left-click worked; Bartender/menu-bar overlay pass-through worked; Copy Diagnostics Summary worked; Quit/Cmd+Q worked; and the user confirmed the DMG-installed app worked normally. |
| v0.4.6 closed release scope is documented | Inspect `Info.plist`, `docs/CURRENT_STATE.md`, `docs/ARCHITECTURE.md`, and this matrix before starting later release work. | Version/build were `0.4.6` / `32`; v0.4.6 is documented as fully released, validated, and closed with the overlay/system UI skip ordering fix, preserved menu/status/Bartender overlay protection, last non-menu focus action/skip diagnostics, overlay/system UI skip reason and candidate diagnostics, last eligible hit-test candidate diagnostics, Copy Diagnostics Summary preservation of useful last non-menu focus results, and no runtime feature expansion beyond the already-validated fix. |
| v0.4.7 public release is documented as superseded | Inspect README and docs before starting later release work. | v0.4.7 / build 33 remains documented as the previous public release and is superseded by v0.5.0 / build 34. |
| v0.5.0 public release is documented as superseded | Inspect README and docs before starting later release work. | v0.5.0 / build 34 remains documented as a public release superseded by v0.6.0 / build 35; public DMG is `HoverClick-0.5.0.dmg`; app identity remains HoverClick / `com.gergoterek.HoverClick`; signing identity remains `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`; the event tap mask remains left mouse down + right mouse down only. |
| v0.6.0 public release is documented | Inspect README and docs before later release work. | v0.6.0 / build 35 is documented as the latest fully validated public release until v0.7.0 is published and the user validates the published DMG; app identity remains HoverClick / `com.gergoterek.HoverClick`; signing identity remains `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`; the event tap mask remains left mouse down + right mouse down only. |
| v0.7.0 release prep is documented | Inspect `Info.plist`, `HoverClick.mm`, README, and docs before release work. | Version/build are `0.7.0` / `36`; v0.7.0 is documented as a release candidate on `release-v0.7.0-prep`, not a published release; Right Click Focus remains default OFF, left-click behavior remains stable, diagnostics distinguish right-click observation, disabled-setting skips, ineligible-target skips, focus attempts, AX results, immediate verification, delayed verification, and last verified success; long-run diagnostics and the Window Server pointer-like pass-through fix are included; Key Focus, Caps Lock Focus, Hover Click Assist implementation, Click-Time Hover Assist, Excluded Apps, Scroll Focus, Finder selection hack, synthetic clicks, event replay, cursor movement, and expanded event handling are excluded. |
| v0.8.0 Sparkle dependency is embedded and signed | Run `scripts/build-app.sh`, then `scripts/verify-app.sh`. | The build downloads only the official pinned Sparkle `2.9.3` archive into ignored `tmp/sparkle/`, verifies SHA-256 `74a07da821f92b79310009954c0e15f350173374a3abe39095b4fc5096916be6`, embeds `Sparkle.framework` in `HoverClick.app/Contents/Frameworks/`, links the app binary with `@rpath/Sparkle.framework`, and signs Sparkle plus nested helpers with the configured Apple Development identity. No Sparkle archive, private key, appcast, release asset, or DMG is tracked or staged. |
| v0.8.0 manual update menu appears | Manual Finder UI validation -- not run automatically. Launch with `scripts/run-app.sh`, open the HoverClick status menu, and click `Check for Updates...`. | The top-level `Check for Updates...` item appears near `About HoverClick...`; selecting it uses Sparkle's standard user-initiated update UI, does not crash if the planned appcast is not live, and does not trigger an automatic update check or background install on launch. |
| v0.8.0 Sparkle configuration is manual-only | Inspect the built app's `Info.plist` with `scripts/verify-app.sh`. | `SUFeedURL` is `https://gergoterek.github.io/HoverClick/appcast.xml`; `SUPublicEDKey` is present; `SUEnableAutomaticChecks`, `SUAutomaticallyUpdate`, and `SUAllowsAutomaticUpdates` are false; no raw GitHub latest-release API, git-push update source, placeholder feed URL, or automatic background update behavior is configured. |
| v0.7.0 release-readiness focus tests | Manual Finder UI validation -- not run automatically. Exercise left-click background focus, right-click background focus, Finder background focus, Chrome background focus, and PDF Expert background focus if PDF Expert is available. | Focus attempts start for normal eligible background windows, the original click events pass through unchanged, right-click context menus remain native, and diagnostics show target detection, `focusAttempt=yes`, AX operation details, immediate verification, delayed verification when needed, and final success or a precise failure reason. |
| v0.7.0 Window Server pass-through diagnostics | Manual Finder UI validation -- not run automatically. Repeat a normal app-window click that previously reported a tiny untitled no-bundle `Window Server` high-layer surface near the click point. | Diagnostics include `Window Server pass-through surface ignored`, `focusAttempt=yes`, and delayed verification passed when immediate verification needs the delayed check. Menu/status UI skip remains protected. |
| App icon source and bundle resource are present | Inspect `assets/HoverClickAppIcon-1024.png`, `Resources/HoverClick.icns`, `Info.plist`, and the built app after `scripts/build-app.sh`. | The source PNG exists at `assets/HoverClickAppIcon-1024.png`; generated `.icns` exists at `Resources/HoverClick.icns`; `CFBundleIconFile` is `HoverClick.icns`; the built bundle contains `HoverClick.app/Contents/Resources/HoverClick.icns`; verification checks pass. |
| DMG volume icon source and resource are present | Inspect `assets/HoverClickDMGVolumeIcon.png`, `Resources/HoverClickDMGVolumeIcon.icns`, and `scripts/package-dmg.sh`. | The dedicated DMG volume icon PNG exists at `assets/HoverClickDMGVolumeIcon.png`; generated `.icns` exists at `Resources/HoverClickDMGVolumeIcon.icns`; `scripts/package-dmg.sh` uses the DMG icon resource for `.VolumeIcon.icns`; the app icon remains `Resources/HoverClick.icns`. |
| App icon smoke test | Manual Finder UI validation -- not run automatically. Build with `scripts/build-app.sh`, verify with `scripts/verify-app.sh`, then use `scripts/run-app.sh` as the approved signed `.app` runtime refresh before inspecting normal macOS icon surfaces. | HoverClick shows the branded app icon; the icon is not a placeholder, stock icon, missing generic app icon, clipped, or visibly distorted. |
| Internal DMG presentation assets verify from command line | Run `scripts/package-dmg.sh`. The script builds, verifies, packages, mounts with `hdiutil -nobrowse -noautoopen`, and detaches without opening Finder. | The generated DMG contains the expected `HoverClick.app`, bundle identifier `com.gergoterek.HoverClick`, matching version/build, valid code signature, `Applications` symlink to `/Applications`, `.VolumeIcon.icns` sourced from `Resources/HoverClickDMGVolumeIcon.icns`, hidden volume icon file flag, and custom volume icon flag. |
| Internal DMG Finder presentation smoke test | Manual Finder UI validation -- not run automatically. After `scripts/package-dmg.sh`, mount the generated DMG manually only when a Finder presentation smoke test is intended. | The mounted DMG volume shows the branded volume icon, `HoverClick.app` is visible, the `Applications` shortcut is available for drag-install, and any future Finder background/layout polish is either verified manually or explicitly documented as not automated. If Finder is configured to show hidden files, `.VolumeIcon.icns` may be visible even though its hidden flag is set. Runtime/event-tap behavior is unchanged. |
| DMG file icon expectation is documented | Inspect docs before release packaging. | The `.dmg` file's own pre-mount Finder icon is not treated as a release feature because custom file icons depend on local resource-fork or extended-attribute metadata that is not a reliable GitHub Release asset property after upload/download; the supported icon polish is the mounted DMG volume icon. |
| App launches as `.app` | Manual Finder UI validation — not run automatically. Run `/Users/gergoterek/Movies/OBS/GPT/HoverClick/scripts/run-app.sh` only when a manual UI test is intended. | The app is opened through `HoverClick.app`, not the raw binary. |
| Exactly one instance runs | Manual Finder UI validation — not run automatically. Run `scripts/run-app.sh` only when a manual UI test is intended, then run `scripts/verify-app.sh`. | Verification reports exactly one HoverClick process. |
| Menubar icon is clear and balanced | Manual Finder UI validation — not run automatically. Compare the HoverClick menu bar status item with nearby native status items after relaunch. | The `cursorarrow.click` template icon is slightly smaller than the previous 17 pt version, still clearer than the original tiny icon, vertically aligned, and not clipped, blurry, or stretched. |
| Menubar icon remains native template | Inspect source and check the current macOS appearance manually. | The icon uses a template SF Symbol with native AppKit tinting and looks correct in the current light/dark menu bar appearance. |
| Menu appears | Manual Finder UI validation — not run automatically. Check the macOS menu bar. | A HoverClick status item appears with feature toggles, `Hover`, `Permissions & Startup`, `Diagnostics`, and `Quit`. |
| Header row appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `HoverClick` and dynamic `v0.7.0` appear on the same compact grey, non-clickable header row; the visible version is generated from `CFBundleShortVersionString`, and no build number appears in the header. |
| Header tooltip is release-independent | Manual Finder UI validation — not run automatically. Hover over the header row and status item. | Tooltip text, if shown by macOS, is stable and release-independent: `HoverClick - Windows-like click focus for macOS.` It does not mention maintenance, updates, releases, `UI-Menubar`, permissions layout, hover submenu, or live version display. |
| About version follows Info.plist | Inspect `Info.plist`, build, relaunch through `scripts/run-app.sh`, then open About manually. | The header shows `v<CFBundleShortVersionString>` and `About HoverClick...` shows `Version <CFBundleShortVersionString>` plus `Build <CFBundleVersion>` from built app metadata; the status-item tooltip and Diagnostics submenu do not show version/build. |
| Permissions & Startup submenu appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Permissions & Startup` appears as a submenu containing Accessibility, Launch at Login, separator, and Open Accessibility Settings at the bottom. |
| Diagnostics submenu appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Diagnostics` appears as a submenu containing `Verbose Diagnostics` and `Copy Diagnostics Summary` without version/build, persistent Event Tap, Last Click, Last Action, Click Detection, or Event Tap detail rows. |
| About item appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu, then choose `About HoverClick...`. | A small native informational alert shows `HoverClick`, `Version 0.7.0`, `Build 36`, `Bundle ID: com.gergoterek.HoverClick`, and `Windows-like click focus for macOS.` It does not open a browser, external link, Finder, or System Settings. |
| Hover submenu appears | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Hover` appears as a parent submenu between `Right Click Focus` and `Permissions & Startup`, with `Hover Click Assist` inside it. |
| Menu text indentation is consistent | Manual Finder UI validation — not run automatically. Open the HoverClick status menu and submenus. | Top-level, submenu, checked, plain, disabled status, and action-icon row labels align with the `Quit` label. `Quit`, `About HoverClick...`, `Open Accessibility Settings`, and `Copy Diagnostics Summary` use action symbols in the left state/checkmark slot, have matching icon-to-text title padding of exactly 1 ASCII space, and do not show extra inline row images. `Accessibility: Granted` shows a native checkmark only when Accessibility is granted. Any remaining shared gutter is native AppKit menu layout, not a custom state. |
| Toggle titles are stable | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Left Click Focus`, `Right Click Focus`, `Hover Click Assist`, and `Launch at Login` do not include `On` or `Off` in their titles. |
| Toggle state uses native checkmarks | Manual Finder UI validation — not run automatically. Open the HoverClick status menu before and after toggling feature items. | Enabled features show the standard left-side macOS checkmark; disabled features are unchecked; Launch at Login may use a mixed state if macOS reports approval is required. |
| Menu item help text is available | Manual Finder UI validation — not run automatically. Hover over relevant status menu items. | Parent submenu items do not show tooltips; specific native menu tooltip/help text appears for relevant actionable and status child items if macOS displays menu item tooltips. |
| Accessibility status is detected correctly | Use the status menu permission item before and after granting permission. | The submenu item reports `Accessibility: Granted` or `Accessibility: Not Granted`; `Accessibility: Granted` shows a native left checkmark only when permission is granted, and logs report trusted `YES` or `NO`. |
| Signing identity is stable | Run `scripts/build-app.sh`, then `scripts/verify-app.sh`. | Codesign authority includes `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`. |
| Rebuild does not change bundle identifier | Run `scripts/build-app.sh` repeatedly and verify `CFBundleIdentifier`. | Bundle identifier remains `com.gergoterek.HoverClick`. |
| Raw binary is never used | Inspect scripts and build commands. | Scripts launch only `HoverClick.app`; no script runs `Contents/MacOS/HoverClick` directly. |
| Event tap installs when allowed | Manual Finder UI validation — not run automatically. Grant Accessibility, then run `scripts/run-app.sh` only when a manual UI test is intended. | Logs include `[HoverClick] event tap installed mode=pass-through-default`; click detection status is available through `Copy Diagnostics Summary`. |
| Missing Accessibility is handled calmly | Remove or do not grant Accessibility, then launch the app. | The app remains open and the menu shows `Accessibility: Not Granted` without repeated prompts. |
| Fresh Accessibility permission flow is understandable | Manual release-readiness validation only. Use a safe clean-user or fresh manual-install scenario when available; do not run this during normal development. Launch only the signed `HoverClick.app` bundle, then grant permission through the normal System Settings flow if prompted. | The app keeps bundle identifier `com.gergoterek.HoverClick`, remains signed by `Apple Development: rizsutt@gmail.com (MVQ5PX4679)`, never uses a raw binary launch, shows understandable Accessibility prompt/status behavior, allows the user to grant Accessibility normally, opens the menu after permission is granted, Left Click Focus works, copied diagnostics show expected Accessibility and event tap state, and a normal quit/relaunch does not unexpectedly re-prompt. Do not use `tccutil reset`; only document risky permission-reset validation separately if the user explicitly chooses it. |
| Left mouse down is observable | With Accessibility granted and click detection active, click anywhere while watching the unified log. | Logs include `[HoverClick] click #... received leftClickFocus=... experimentalHoverClickAssist=... raw=(x,y) converted=(x,y)`. This is event detection only. |
| Technical click detection is not a persistent row | Manual Finder UI validation — not run automatically. Open the status menu and submenus. | `Event Tap` / `Click Detection` is not shown as a separate persistent menu row. |
| Last action is not a persistent row | Manual Finder UI validation — not run automatically. Open the status menu and submenus. | `Last Click` / `Last Action` is not shown as a separate persistent menu row. |
| Copy Diagnostics Summary works | Manual Finder UI validation — not run automatically. Click `Diagnostics` > `Copy Diagnostics Summary`, then paste into a text field. | The copied text includes app name, bundle identifier, Accessibility, Launch at Login, click detection, last handled action, preserved last focus action/skip, last non-menu focus action/skip, last focus decision detail, last right-click focus decision, stable last real/background click decision fields, overlay/system UI skip detail, last eligible hit-test candidate, recent non-menu mouse-down decision history, aggregate callback/focus/skip counters, persistent background-focus trigger/target/frontmost-before/activation/AX/immediate/delayed/final verification/failure/success fields, feature states, Hover Click Assist setting plus no-op runtime behavior, expanded event tap lifecycle fields, last left/right mouse-down seen, event tap mask, safety note, and concise known limitations; version/build are available from `About HoverClick...` instead of duplicated here; the menu row keeps exactly 1 ASCII space of icon-to-text title padding. |
| Event tap lifecycle diagnostics are complete | Manual Finder UI validation — not run automatically. Copy diagnostics after launch and again after at least one left and right mouse-down. | Diagnostics include event tap requested state, object/source presence, port/source validity, believed installed/enabled state, detected enabled state when available, last event tap callback, last left/right mouse-down timestamps, last tap recovery attempt/result, last focus decision detail, last right-click focus decision, persistent last background-focus execution fields, and last verified successful background focus. |
| Background focus diagnostics persist across menu clicks | Manual Finder UI validation — not run automatically. Focus a background window with left-click or right-click focus, then open the HoverClick menu and copy diagnostics. | `Last handled action` may reflect a menu/overlay click, but `Last non-menu focus action/skip`, `Last real/background click decision`, recent non-menu mouse-down decision history, `Last background focus attempt`, execution details, result, verification, delayed verification, and `Last verified successful background focus` still describe the most recent meaningful click path instead of being erased by the menu interaction. |
| Duplicate event tap install is skipped | Click Accessibility status refresh while click detection is already active. | Logs include `event tap already installed; skipping duplicate install` and only one HoverClick process remains. |
| Event tap timeout disabled path | Manual stress validation only. If macOS disables the tap by timeout, watch logs and copy diagnostics. | Logs include `event tap disabled by timeout`; when user intent is still enabled, recovery either logs `event tap re-enabled after timeout` for a valid existing tap or logs recreation when the port/source is invalid or re-enable fails. Diagnostics show the last callback and recovery result. |
| Event tap user-input disabled path | Manual system validation only. If macOS disables the tap by user input, watch logs and copy diagnostics. | Logs include `event tap disabled by user input`; when user intent is still enabled, recovery either re-enables the existing tap or recreates the left/right mouse-down-only tap. Diagnostics show the actual/believed tap state and recovery result. |
| Long-run click focus remains available | Manual long-run validation only. Launch with `scripts/run-app.sh`, confirm left and right click focus work, leave HoverClick running through extended idle and sleep/wake or lock-unlock if practical, then test left and right click focus again. | Left and right click focus continue to work. If callbacks still arrive but focus does not happen, copied diagnostics immediately show whether each recent non-menu mouse-down reached target resolution and policy, whether an eligible candidate was found, the exact skip reason, whether overlay/system UI or compact-popup classification was involved, whether a focus attempt started, AX operation summary, immediate and delayed verification, final result, callback/focus/skip counters, and stable last real/background click fields that are not overwritten by copying diagnostics. |
| Launch at Login menu item appears | Manual Finder UI validation — not run automatically. Open `Permissions & Startup`. | `Launch at Login` appears inside the submenu as an independent checked/unchecked menu item. |
| Launch at Login toggles on | Manual Finder UI validation — not run automatically. Click `Launch at Login`, close the menu, then reopen it. | The item remains checked when ServiceManagement reports the main app login item is enabled. |
| Launch at Login toggles off | Manual Finder UI validation — not run automatically. Click `Launch at Login` again, close the menu, then reopen it. | The item remains unchecked when ServiceManagement reports the main app login item is not registered. |
| Launch at Login requires approval | If macOS reports the login item requires user approval, inspect the menu/logs. | The menu uses a mixed state and logs `requires approval` without opening System Settings automatically. |
| Open Accessibility Settings is explicit | Manual Finder UI validation — not run automatically. Open `Permissions & Startup` without clicking the settings item. | System Settings does not open unless `Open Accessibility Settings` is clicked explicitly; the item shows its action symbol in the left state/checkmark slot with exactly 1 ASCII space of title padding. |
| Quit remains an action | Manual Finder UI validation — not run automatically. Open the HoverClick status menu. | `Quit` shows exactly one icon in the left state/checkmark slot, has exactly 1 ASCII space of title padding and no inline row icon, remains unchecked, and keeps the Cmd+Q shortcut. |
| Mouse movement over background window | Manual Finder UI validation — not run automatically. Move the pointer over a background Finder/Chrome/iTerm window without clicking. | No focus change occurs, and no hover-to-focus logs appear. |
| Mouse movement over current front app | Move the pointer within the current frontmost app. | Focus remains unchanged and no AX focus/raise/activate path runs from mouse movement. |
| Left click focus remains available | With `Left Click Focus` checked, click a visible background Finder/Chrome/iTerm window. | The target window focuses before click delivery and the original click event is passed through unchanged. |
| Right Click Focus defaults off | Launch HoverClick after a fresh/default configuration, then inspect the status menu. | `Right Click Focus` appears as an enabled, unchecked menu item. |
| Right Click Focus off preserves normal right-clicks | With `Right Click Focus` unchecked, right-click a visible background Finder/Chrome/iTerm window. | HoverClick does not run the focus path for the background target, the original right-click event passes through unchanged, and the normal context menu behavior is preserved. Copied diagnostics show the right mouse down was seen and the right-click-specific decision says the event was skipped because Right Click Focus was disabled. |
| Right Click Focus toggles independently | Toggle `Right Click Focus` while `Left Click Focus` stays on, then toggle `Left Click Focus` while `Right Click Focus` stays off or on. | Each setting updates independently; left-click focus remains controlled only by `Left Click Focus`, and right-click focus remains controlled only by `Right Click Focus`. |
| Right Click Focus on focuses background window | With `Right Click Focus` checked, right-click a visible background Finder/Chrome/iTerm window. | The target window focuses before right-click delivery, logs use the `right-click` trigger, the original right-click event passes through unchanged, and the normal context menu can appear. Copied diagnostics show right mouse down seen, target app/window detection, focus attempt, AX operation result, immediate verification and delayed verification if needed, and last verified successful background focus if verification passed. |
| Right Click Focus on preserves active-window context menu | With `Right Click Focus` checked, right-click inside the already-frontmost app. | HoverClick treats the target as already frontmost, returns the original right-click event unchanged, and the context menu opens normally. Copied diagnostics identify the skip as already-frontmost rather than a focus attempt failure. |
| Right Click Focus ineligible-target diagnostics | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, right-click menu bar, status item, system UI, popover, or compact overlay surfaces. | The UI receives the original right-click unchanged. Copied diagnostics distinguish the event tap/right mouse down observation from a skip because the AX target or topmost CoreGraphics window was not an eligible normal app/window target. |
| Right Click Focus context menu regression | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, right-click background and frontmost Finder/Chrome/iTerm windows. | Normal context menus appear where the app would normally show them. HoverClick does not consume, replay, replace, or post the right-click event. |
| Right Click Focus double-click regression | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, double-click normal items in Finder and text/content areas in another app after the target is frontmost. | Double-click behavior remains native. HoverClick does not add mouse-up, mouse-dragged, synthetic click, event replay, or cursor movement behavior. |
| Right Click Focus background drag/selection regression | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked and unchecked, repeat background text selection, item drag, and active-window drag checks. | Existing background drag/selection behavior is preserved as a known regression-test area. The first-drag limitation may still exist; this batch does not attempt to fix it. |
| Finder context-menu follow-up left click | Manual Finder UI validation -- not run automatically. With `Right Click Focus` checked, right-click a file in a background or already-frontmost Finder window to open its context menu, then left-click another file. | HoverClick does not perform AX hit-testing, focus, raise, or activation work on the first left click after a recent Finder right-click while Finder is already frontmost; the original left click passes through unchanged, the context menu dismisses normally, and the file receives the click or selection as close as possible to native Finder behavior without synthetic click replay. |
| Old persisted Hover Focus setting is ignored | Set or leave any previous `HoverFocusEnabled` user default, then launch HoverClick normally. | The old setting cannot re-enable mouse-move-to-focus behavior and no Hover Focus menu item appears. |
| Hover Click Assist defaults off | Launch HoverClick after a fresh/default configuration. | Menu shows `Hover Click Assist` unchecked. |
| Hover Click Assist off is a hard-off switch | Manual Finder UI validation — not run automatically. With `Left Click Focus` checked and `Hover Click Assist` unchecked, click a visible background Finder/Chrome/iTerm window while watching logs. | The target window may focus through the left-click focus path, the original event passes through unchanged, logs include `experimentalHoverClickAssist=OFF` and `Experimental Hover Click Assist OFF: no assist path scheduled`, and no synthetic click, cursor movement, mouse-move, hover, Gmail, animated-button, or delayed assist path runs. |
| Hover Click Assist depends on Left Click Focus | Toggle `Hover Click Assist` on, then turn `Left Click Focus` off and on again. | `Hover` is disabled while `Left Click Focus` is off, `Hover Click Assist` cannot be toggled through the UI, and the previous checked state is restored when `Left Click Focus` is turned back on. |
| Hover Click Assist is inert in the current build | Turn `Hover Click Assist` on, then move over background windows and click normally. | No mouse-move focus, synthetic click, cursor movement, replacement event, or assist behavior occurs. Stable left-click focus still returns the original event unchanged; diagnostic delayed verification may run only after immediate background-focus verification fails. |
| Verbose Diagnostics toggles | Toggle `Verbose Diagnostics` in the Diagnostics submenu. | The native checkmark changes and logging detail follows the setting. |
| Right-click event tap remains click-only | Inspect source for right-click support. | Right Click Focus uses `kCGEventRightMouseDown`; no mouse-moved, scroll, synthetic click, cursor movement, replacement event, or delayed assist path is added. |
| Event tap mask remains narrow for v0.7.0 | Inspect `HoverClick.mm` and copied diagnostics. | The event tap mask remains left mouse down + right mouse down only; no synthetic clicks, event replay, cursor movement, mouse-move focus, scroll focus, `mouseDragged`/`mouseUp` handling, `CGEventPost`, `CGEventCreateMouseEvent`, `CGDisplayMoveCursorToPoint`, or `CGWarpMouseCursorPosition` were added. |
| Scroll Focus remains unimplemented | Inspect source for `kCGEventScrollWheel`, scroll focus toggles, or scroll focus handlers. | No scroll focus path exists in the current source-first docs or runtime. |
| Modifier Key Focus remains unimplemented | Inspect source and menus for modifier-key or hold-to-focus controls. | Modifier Key Focus / Hold-to-Focus remains a future idea only and no current event-tap path or menu control exists for it. |
| Failed background drag experiment is not reused | Inspect source and docs before any future click-and-drag work. | The failed 35 ms background drag assist / activation-settle approach is documented as not reusable; no background click-and-drag assist, synthetic click, cursor movement, event replay, `mouseDragged`, or `mouseUp` path exists in the current build. |
| Trigger controls stay separate | Inspect menus and source. | Current top-level feature controls include Left Click Focus and Right Click Focus; `Hover Click Assist` lives under `Hover`; technical click detection appears only in the copied diagnostics summary; Scroll Focus is not presented as a current feature. |
| Background Finder window focuses | Manual Finder UI validation — not run automatically. Click a visible background Finder window. | Logs show target app Finder, target window, `AXRaise`, app activation, immediate verification, delayed verification only if needed, and Finder becomes frontmost. |
| Background Chrome window focuses | Manual Finder UI validation — not run automatically. Click a visible background Chrome window. | Logs show target app Chrome, target window, action attempts, immediate verification, delayed verification only if needed, and Chrome becomes frontmost. |
| Chrome-to-Finder delayed focus verification passes | Manual Finder UI validation -- not run automatically. From Chrome frontmost, use left-click and right-click background focus on Finder and copy diagnostics. | Manual main validation passed for both triggers: activation attempted=yes returnValue=yes, AX operations attempted:success, immediate frontmost remained Chrome, delayed verification passed after 0.20s with Finder frontmost, result success, and verification delayed passed. |
| Background iTerm window focuses | Manual Finder UI validation — not run automatically. Click a visible background iTerm window. | Logs show target app iTerm, target window, `AXRaise`, activation, immediate verification, delayed verification only if needed, and iTerm becomes frontmost. |
| Background text first-drag limitation | Manual Finder UI validation -- not run automatically. With `Left Click Focus` checked, place a text-heavy editor, browser, or text field in the background, then start a click-drag selection from inactive state. | The window should focus and the original mouse events should pass through unchanged. Some apps may still treat the first mouse-down as activation-only, so first-drag text selection may require a second drag unless a future safe non-replay fix is proven. |
| Active text drag still works | Manual Finder UI validation -- not run automatically. With the same text window already frontmost, click-drag text. | Text selection or drag behaves normally; HoverClick does not interfere because the target app is already frontmost. |
| Menu/status clicks are ignored | Click or right-click the menu bar or HoverClick status item. | Logs show the target was ignored as own app, menu, or status UI. |
| Non-layer-0 overlay clicks pass through | Manual Finder UI validation -- not run automatically. Click menu bar, status-item, app popover, or overlay UI shown above a normal window. | HoverClick does not focus or click the window behind protected menu/status/system UI, compact popup-style overlays, or popovers; the original click event passes through unchanged, and diagnostics show the overlay/system UI skip reason plus topmost owner/window/layer/title/bounds and AX role/subrole/app detail. |
| Large transparent non-normal overlays do not suppress normal windows | Manual Finder UI validation -- not run automatically. If diagnostics previously reported `topmost overlay or system UI click ignored` for an ordinary app-window click, repeat that ordinary background-window click. | When AX resolves a normal eligible app-window candidate beneath a large non-normal top window, HoverClick does not skip solely because the CoreGraphics layer is non-zero. Diagnostics show `not skipped` overlay detail, the eligible hit-test candidate, and a background focus attempt for the target app. |
| Tiny Window Server pointer-like surface does not block normal target | Manual Finder UI validation -- not run automatically. Repeat a normal background-window click that previously reported a 35 x 49-ish untitled no-bundle `Window Server` topmost window and `topmost non-normal popup/menu layer is compact and likely interactive`. | If AX resolves a normal app target such as Finder, Chrome, or PDF Expert, diagnostics report `Window Server pass-through surface ignored`, `compactPopup` is pass-through/ignored rather than a blocking skip, `focusAttempt=yes`, AX operations are attempted, and immediate or delayed verification gives the specific result. |
| Bartender expanded icons pass through | Manual Finder UI validation -- not run automatically. With Bartender 6 expanded/overflow menu bar icons visible, click hidden/overflow icons in the expanded row. | The Bartender icon receives the original click normally; HoverClick does not focus or click the window behind the Bartender overlay, and diagnostics identify the protected menu-bar/system UI or compact-overlay skip. |
| Popover and transient menu UI are ignored | Click menu, popover, or transient app UI when visible. | HoverClick passes the event through without stealing focus away from the current app. |
| Current front app gets minimal action | Click the already-front app. | Logs show the target app is already frontmost and the event passes through. |
| Rapid clicking between apps | Click quickly between Finder, Chrome, iTerm, and Terminal. | Logs keep sequence ids distinct; stale delayed verification callbacks do not overwrite a newer background-focus attempt. |
| Target app disappears after click | Manual validation only. Quit or close a target app immediately after click-to-focus. | HoverClick does not crash; delayed verification either reports the current frontmost app as a failed verification or is skipped as stale if a newer attempt has replaced it. |
| No click behavior changes | Use normal clicks while the event tap is enabled. | The event tap returns the original event unchanged after any focus attempt. |
| No drag behavior changes | Drag windows, text selections, or sliders while the event tap is enabled. | Drag behavior remains unchanged. |
| No window movement or resize | Click background windows and drag normally. | HoverClick logs focus/raise/activation only; windows do not move or resize. |
| No synthetic click | Search source and test normal click behavior. | No `CGEventPost` or synthetic mouse event path exists; the physical click is passed through. |
| No duplicate process or tap | Run `scripts/run-app.sh`, then `scripts/verify-app.sh`; refresh Accessibility status from the menu. | Exactly one app process is running, and duplicate event tap installs are skipped. |
| Menu items stay enabled where applicable | Open the status menu before and after toggling permission, diagnostics state, and Left Click Focus. | Permissions & Startup, Diagnostics, Accessibility, Left Click Focus, Right Click Focus, Verbose Diagnostics, Copy Diagnostics Summary, Open Accessibility Settings, and Quit are not accidentally greyed out. `Hover` and `Hover Click Assist` are enabled only when Left Click Focus is enabled. |
| Multi-monitor negative coordinates | Manual Finder UI validation — not run automatically. Click windows on secondary displays that use negative coordinates, if available. | Logs show raw and converted coordinates, target lookup succeeds, and no Retina pixel conversion is needed. |
| Focus result is not a false positive | Review logs after a background-window click. | Success is based on target resolution plus action and immediate or delayed frontmost verification logs, not only event observation. |

## Stable Checkpoint Manual Validation

Manual Finder UI validation -- not run automatically.

The current stable checkpoint has been manually validated with:

- Left Click Focus ON focuses background windows and returns the original left-click event unchanged.
- Left Click Focus OFF passes left-clicks through without running the focus path.
- Right Click Focus ON focuses the target background window first and returns the original right-click event unchanged so the normal context menu works.
- Right Click Focus OFF passes right-clicks through without running the focus path.
- Finder context-menu follow-up left-click passes through without AX hit-testing after a recent Finder right-click while Finder is frontmost.
- Bartender/menu-bar overlay clicks pass through without focusing the window behind the overlay.
- The HoverClick status menu, Diagnostics submenu, Copy Diagnostics Summary, Permissions & Startup submenu, Open Accessibility Settings action, About item, and Quit/Cmd+Q remain present and uncluttered.
- Launch at Login works.
- Mouse movement alone does not focus windows.
- Known limitations are documented as limitations, not failures: Finder right-click actual selection may not change, and background text first-drag can require a second drag in some apps.
- Accessibility permission did not reappear.
- No synthetic click, cursor movement, `CGEventPost`, `CGDisplayMoveCursorToPoint`, `kCGEventMouseMoved`, or scroll-focus behavior exists.
- For the closed v0.4.6 release on main, the overlay/system UI skip ordering fix was manually validated: a large transparent/non-interactive non-layer-0 window did not suppress a normal eligible Chrome app/window target, menu/status/Bartender overlay protection remained intact, last non-menu focus action/skip and eligible hit-test candidate diagnostics were preserved, activation attempted=yes returnValue=yes, AX operations attempted:success, delayed verification passed after 0.20s, result success, and verification delayed passed.
- The published v0.4.6 DMG smoke test passed with SHA-256 `4e31b9196458e326bc794dbeb33525ce4a8d2b58fe463de0e9c3c789d3a6c076`, and the user confirmed the installed app worked normally.
